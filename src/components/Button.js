import React from "react";

function Button(props) {
    return (
        <div>
            <button onClick={props.Giphy}>This is the aforementioned button.</button>
        </div>
    );
};

export default Button;